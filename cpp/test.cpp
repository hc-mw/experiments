// Given Array a of N integers. Given Q wqureis


#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[]) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int arr[3] = {0, 0, 0};

	arr['a' - 'a']++;

	for (int& i : arr)
		cout << i << endl;

	return 0;
}